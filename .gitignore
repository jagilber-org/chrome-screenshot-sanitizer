# ===================================================================
# RUNTIME ARTIFACTS - NEVER COMMIT
# ===================================================================
# These directories contain runtime-generated data, test artifacts,
# and temporary files that MUST NEVER be committed to the repository.

# Temporary directories and scratch space (ALL test artifacts)
tmp/
tmp/**
tmp/**/*.tmp

# Runtime data and state directories
data/
data/**
!data/.keep
memory/
memory/**
!memory/.keep
metrics/
metrics/**
!metrics/.keep

# Log files and runtime logs
logs/
logs/**
!logs/.keep
!logs/trace/.keep
logs/trace/*.jsonl

# Test run sentinel / status markers (ALL test markers)
.test-run-complete.*.marker
.test-run-complete.*
.test-run-status.json
.test-run-complete.latest
.baseline.sentinel

# Coverage reports
coverage/
.nyc_output/

# Backup directories
backups/
*_backup_*/

# ===================================================================
# SENSITIVE DATA - NEVER COMMIT
# ===================================================================

# Screenshots with sensitive data (CRITICAL: Never commit unsanitized screenshots)
images/*.png
images/*.jpg
images/*.jpeg
images/*.gif
images/*.webp
# Allow sanitized examples only
!images/examples/*-sanitized.png
!images/examples/*-sanitized.jpg
!images/.gitkeep

# Environment files with secrets (CRITICAL: Never commit real .env files)
.env
.env.*
!.env.example

# Sensitive configuration files (CRITICAL: Never commit real replacement configs)
replacements-*.json
!replacements-azure-portal.template.json
# Note: replacements-azure-portal.json is intentionally gitignored despite earlier config

# Browser debug profiles and sessions
*Debug/
EdgeDebug/
ChromeDebug/
.debuggerinfo

# Temporary files and sanitization outputs
*.tmp
*.temp
temp/
$env:TEMP/
*-sanitize.js

# PowerShell
*.ps1.swp
*.psm1.swp

# Git
.git/

# VS Code
.vscode/
*.code-workspace

# Node modules (if using npm for Chrome MCP)
node_modules/
package-lock.json

# OS files
Thumbs.db
.DS_Store

# Exclude azure-resource-explorer (not part of portfolio)
projects/azure-resource-explorer/
