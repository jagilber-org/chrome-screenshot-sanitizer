# ===================================================================
# RUNTIME ARTIFACTS - NEVER COMMIT
# ===================================================================
# These directories contain runtime-generated data from sanitization
# operations and MUST NEVER be committed to the repository.

# Project outputs (sanitized screenshots - may still contain sensitive data)
projects/*/outputs/*.png
projects/*/outputs/*.jpg
projects/*/outputs/*.jpeg
projects/*/outputs/*.gif
projects/*/outputs/*.webp
# Preserve directory structure with .gitkeep
!projects/*/outputs/.gitkeep

# Temporary PowerShell artifacts
tmp/
*.tmp
*.temp
temp/

# PowerShell transcripts and logs (may contain sensitive runtime data)
PowerShell_transcript.*.txt
logs/
*.log

# Generated sanitization scripts
*-sanitize.js

# Backup directories
backups/
*_backup_*/

# ===================================================================
# SENSITIVE DATA - NEVER COMMIT
# ===================================================================

# Screenshots with sensitive data (CRITICAL: Never commit unsanitized screenshots)
images/*.png
images/*.jpg
images/*.jpeg
images/*.gif
images/*.webp
# Allow sanitized examples only
!images/examples/*-sanitized.png
!images/examples/*-sanitized.jpg
!images/.gitkeep

# Environment files with secrets (CRITICAL: Never commit real .env files)
.env
.env.*
!.env.example

# Sensitive configuration files (CRITICAL: Never commit real replacement configs)
replacements-*.json
!replacements-azure-portal.template.json

# Browser debug profiles and sessions
*Debug/
EdgeDebug/
ChromeDebug/
.debuggerinfo

# PowerShell editor temporary files
*.ps1.swp
*.psm1.swp

# ===================================================================
# DEVELOPMENT ARTIFACTS
# ===================================================================

# VS Code
.vscode/
*.code-workspace

# Node modules (Chrome MCP integration)
node_modules/
package-lock.json

# OS files
Thumbs.db
.DS_Store

# Exclude specific projects (not part of portfolio)
projects/azure-resource-explorer/

